# Installation

1. `uv venv`: First initialize virtual environment
2. `source .venv/bin/activate`: Then activate virtual environment
3. `uv add requirements.txt`: Next, add all installed packages the project requires